import { dueDetails } from './due.js';

export function itemDate(item,sortMode='created'){let edited=sortMode==='edited',date=new Date(edited?lastEditedAt(item):item.createdAt),label=Number.isNaN(date.getTime())?'':new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric'}).format(date);return edited&&label?`Edited ${label}`:label}
export function lastEditedAt(item){return Math.max(Number(item.lastEdited)||0,Number(item.updatedAt)||0,Number(item.createdAt)||0,...(item.recordings||[]).map(part=>Number(part.updatedAt||part.createdAt)||0),...(item.attachments||[]).map(photo=>Number(photo.updatedAt||photo.createdAt)||0))}
export function sortSavedItems(items,sortMode='created'){let timestamp=item=>sortMode==='edited'?lastEditedAt(item):Number(item.createdAt)||0;return [...items].sort((a,b)=>timestamp(b)-timestamp(a))}
export function itemPreview(item){if(item.type==='VOICE'){let count=item.recordings?.length||1;return `${count} recording${count===1?'':'s'}`}let text=String(item.text||'').trim().replace(/\s+/g,' ');if(!text&&item.type==='TASK')text=String(item.checklist?.find(step=>!step.completed)?.text||item.checklist?.[0]?.text||'').trim();return text.slice(0,100)}
export function groupTasks(tasks,now=new Date()){let groups={overdue:[],today:[],upcoming:[],none:[]};for(const task of tasks){let due=dueDetails(task.dueAt,now);groups[due?.status||'none'].push(task)}return groups}
